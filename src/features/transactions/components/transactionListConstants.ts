import {space} from '../../../design/tokens/spacing';

/** Fixed row metrics for FlatList getItemLayout — keep in sync with row/header UI. */
export const ROW_HEIGHT = 80;
export const ITEM_GAP = space[3];
